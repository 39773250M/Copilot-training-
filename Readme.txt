1. Angular project creation 
        npm uninstall @angular/cli

        npm install -g @angular/cli@12.2.18

        ng new my-angular-project
        cd my-angular-project
        npm install 
        ng serve


2. JSON server setup
        npm install -g json-server@0.17.1

        json-server db.json
        
        http://localhost:3000

        for more information on json-server visit 

        https://www.npmjs.com/package/json-server

 3. Install REST Client / CURL / Postman 
    create a test.http to make get /post /put calls 

4. install codex extension  .n

5.















